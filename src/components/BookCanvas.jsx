import React from 'react';import {ImagePlus} from 'lucide-react';
export default function BookCanvas({book,index,onArt,reading=false}){const page=book.pages[index];if(!page)return null;return <div className={'book-spread '+(reading?'reading-spread':'')} aria-label={`Spread ${index+1}: ${page.title}`}><div className="book-page illustration-page">{page.image?<img src={page.image} alt={page.alt||page.scene||page.title}/>:<button className="add-art" onClick={onArt}><ImagePlus size={36}/><span>Add an illustration</span><small>Generate art or upload your own</small></button>}<span className="page-number art-number">{index*2+1}</span></div><div className="book-page text-page"><div className="page-copy"><h2>{page.title}</h2><p>{page.text}</p></div><img className="botanical" src="/art/coastal-flowers.webp" alt=""/><span className="page-number">{index*2+2}</span></div></div>;}
